# HA shape: replicas=3 + Redis + RWO git (object-store-backed; no RWM). Render-only check.
relayUrl: wss://crew.example.com
ownerPubkey: "abcdef0123456789abcdef0123456789abcdef0123456789abcdef0123456789"
replicaCount: 3
secrets:
  existingSecret: crew-secrets
externalPostgresql:
  url: "postgres://crew:pw@postgres.example.com:5432/crew"
externalRedis:
  url: "redis://:pw@redis.example.com:6379"
s3:
  bucket: "crew-media"
persistence:
  git:
    enabled: true
    accessMode: ReadWriteOnce
    size: 50Gi
podDisruptionBudget:
  enabled: true
  minAvailable: 2
