# Griddle rebrand + EKS deploy

- **Date:** 2026-08-23 KST
- **Started:** 01:25 KST
- **Topic:** Forked block/buzz → patrickrho-patty/buzz, rebranded to "Griddle" (Patty's internal Slack replacement), built + deployed on EKS, signed/notarized macOS app for local testing.

---

## 01:25 KST — Kickoff
Multi-hour session. Captured the whole arc end-to-end so nothing is lost at handoff. Repository at `/Users/patrickrho/projects/buzz` (fork `patrickrho-patty/buzz` on GitHub). Full timeline below.

---

## Decision: product name "Griddle"
- Company name is "Patty"; product name picked by user after rejecting HQ (multi-office confusion), PattyChat (sounds like a customer product), Hive (no Patty connection), Smashed/Grill/Skillet/Bun/Sesame/Tricia. Settled on **Griddle** — neutral, internal-feeling, evokes "where patties sizzle" without containing "Patty".

## Branding pass — code & assets
- `web/index.html` `<title>Buzz</title>` → `<title>Griddle</title>`. **Missed `desktop/index.html` initially** — separate build, same `<title></title>` blank, same `/buzz.svg` favicon link. Caught later.
- `desktop/src-tauri/tauri.conf.json` `productName: "Buzz"` → `"Griddle"`.
- `crates/buzz-relay/src/nip11.rs` NIP-11 `name: "Buzz Relay"` → `"Griddle Relay"`; description updated.
- `Dockerfile` OCI labels retitled.

## Iconography — vectorized via potrace
- User provided `brand/griddle_logo_1.png` (1254×1254 square mark) and `brand/griddle_logo_2.png` (1448×1086 wide lockup with "by Patty").
- `desktop/src-tauri/icons/*` regenerated by `pnpm tauri icon griddle_logo_1.png` (Tauri regenerates macOS .icns, Windows .ico, Square*Logo, iOS AppIcon set, Android mipmap set from one source — fork-friendly).
- `web/src/assets/app-icon@3x.png`, `desktop/public/app-icon@2x.png`, `@3x.png` resized manually with ImageMagick.
- `web/public/favicon.png` (new — web had no favicon) + `<link>` added.
- `desktop/public/buzz.svg` → SVG wrapping the traced logo (initially a base64 wrapper; later replaced with a real vector).
- `BuzzMark.tsx` geometry replaced via potrace path data: `brew install potrace`, threshold grayscale, trace to SVG, extract path, bake into component with `viewBox="0 0 1254 1254"` `fill="currentColor"`. Kept API (`{ className }`) unchanged.
- Wordmark assets exported: `web/src/assets/griddle-logo-wide.png`, `desktop/public/griddle-logo.png`.

## CI workflow
- `.github/workflows/ecr-build.yml` (workflow_dispatch) — builds linux/amd64 from Dockerfile, pushes to ECR `361645878435.dkr.ecr.ap-apron64.amazonaws.com/griddle:v0.1.0`. Auth via AWS creds stored as `gh secret set AWS_ACCESS_KEY_ID` / `AWS_SECRET_ACCESS_KEY` (fork's repo secrets).
- Forks need: `GHCR_IMAGE` repo var (per upstream comment) or use this new workflow.

## Deployment to EKS (`rho-cluster`, ap-northeast-2)
- Built cluster: 4 amd64 nodes, Helm 4.1.3, no default StorageClass (gp2 existed, 0 other PVCs in cluster).
- GitHub Actions build: ~20 min via GH cache.
- Chart: `oci://ghcr.io/block/buzz/charts/buzz --version 0.1.7`.
- **Quickstart profile initially (bundled PG/Redis/MinIO) — no external services exist for this app yet.** Values in `deploy/griddle-eks.yaml`.
- Helm install blocked on bundled Postgres/Redis/MinIO using PVCs without `storageClassName` (cluster gp2 had no `is-default-class` annotation). **Fixed by annotating `gp2 storageclass.kubernetes.io/is-default-class=true`** (zero-impact: no other workloads use PVCs).
- Chart schema rejected `minio.persistence.storageClass` key (bundled MinIO template doesn't plumb storageClass; relies on cluster default).
- Rolling update deadlock on R R/Git volume (PVC + 2 pods → Multi-Attach error). **Fixed by `kubectl scale --replicas=0` then `1`.** Will recur on every rollout at replicaCount=1; future fix is either migration to ReadWriteMany (EFS) or a proper `maxUnavailable` strategy.
- Sidecar binaries (`buzz-acp`, `buzz-agent`, `buzz-backend-kubernetes`, `buzz-dev-mcp`, `git-credential-nostr`) must be staged to `desktop/src-tauri/binaries/<bin>-<target>` before `pnpm tauri build` — missed on first attempt, caused `failed to build Tauri application: resource path binaries/buzz-acp-aarch64-apple-darwin doesn't exist`.

## TLS / DNS for griddle.patty.io
- User added CNAME `griddle.patty.io → k8s-rhoalbgroup-...elb.amazonaws.com` in Cloudflare (patty.io zone managed elsewhere).
- External-dns not actually running here (empty namespace), no Route53 zone for patty.io in this account, so DNS automation unavailable.
- Requested ACM cert via `aws acm request-certificate --domain-name griddle.patty.io --validation-method DNS`. Got validation CNAME.
- User added the validation CNAME in Cloudflare. Cert issued.
- Upgraded ingress with `alb.ingress.kubernetes.io/certificate-arn: arn:aws:acm:ap-northeast-2:361645878435:certificate/5cca1da8-...`, added `listen-ports: [{HTTP:80},{HTTPS:443}]`, `ssl-redirect: "443"`.
- **Shared ALB group limitation**: the `rho-alb-group` is shared with other apps' ingresses; ALB-level ssl-redirect applies to all host rules. Currently HTTP→HTTPS forces for griddle.patty.io. Can't get rid of it without splitting to a dedicated ALB group.
- ACM cert requester Issuer ID (from App Store Connect): `bd29c59b-2e2f-4f83-9dcc-ee7779ed61df`.

## Identity / invites / owner
- Relay owner private key generated at planning time → `/tmp/griddle-key.pem` (originally volatile), pubkey `ebf0b5847e34a95716bca3cd83c1cd91efd8f6b610cc62b4531ec5e7d9825026`.
- Backed up to `~/griddle-backups/griddle-owner-key.pem` + `griddle-owner-privkey-hex.txt` (chmod 600).
- Relay identity key (autogenerated by chart at install, in secret `griddle-buzz-relay` ns `griddle`): backed up to `~/griddle-backups/griddle-identity-key.txt` + full `griddle-secrets-backup.yaml`. **Critical: losing this breaks NIP-43 signature verification.**
- First invite minted: `v2.LSTJU8GkWmhRMmIbtmTJU_T4-P4ZJaKQlBmv0MCPzV8` (7-day TTL, 25 uses). Landing page: `https://griddle.patty.io/invite/v2.LSTJU8...`. End-to-end verified — `<title>Griddle</title>` returned.
- Minter script: `~/griddle-backups/mint-invite-nip98.py` (Python with secp256k1 + NIP-98 auth). Workaround for Python TLS cert chain trust: pass `context=ssl._create_unverified_context()` to urlopen. **curl works without the workaround.**
- Helper rust bin in `/tmp/nip98test/` signs NIP-98 events cleanly using the actual `nostr` crate (Python hand-roll of BIP-340 Schnorr initially hit event deserialization errors because of subtle JSON shape mismatches; the rust helper fixed it).

## macOS desktop — code signing & notarization
- Keychain holds `Developer ID Application: Patty Co.,LTD (S37644C7R8)` (`security find-identity -v -p codesigning`).
- Notarytool API key at `resources/certs/apple/AuthKey_X3NN5MNGF2.p8`; Issuer ID `bd29c59b-...`. Stored credentials as keychain profile `griddle-notary`.
- Build steps:
  1. `cargo build --release -p buzz-acp -p buzz-agent -p buzz-backend-kubernetes -p buzz-dev-mcp -p buzz-cli -p git-credential-nostr`
  2. Stage binaries into `desktop/src-tauri/binaries/<bin>-aarch64-apple-darwin`.
  3. `pnpm tauri build --bundles app,dmg` (or `--bundles app` first to get the .app, then rebuild DMG with `bundle_dmg.sh` to replace the .app inside the DMG with the **signed** copy — `pnpm tauri build --bundles app,dmg` re-bundles and discards the signed copy, so the notarization fails).
- Codesign: `codesign --deep --force --options runtime --entitlements desktop/src-tauri/Entitlements.plist --sign "$IDENT" Griddle.app` AND **each sidecar binary individually** before the outer app signature. First notarization failed because sidecars were only ad-hoc signed.
- Notarize: `xcrun notarytool submit Griddle_0.5.18_aarch64.dmg --keychain-profile griddle-notary --wait`.
- Staple: `xcrun stapler staple Griddle_0.5.18_aarch64.dmg` (DMG ticket covers the app inside). For the loose `.app`: notarial is `xcrun notarytool submit` of a `ditto -c -k --keepParent` zip, then `xcrun stapler staple Griddle.app`. **Loose .app cannot be stapled inside the DMG (read-only)**.
- Verify: `spctl --assess --verbose=4 --type execute Griddle.app` → `accepted / source=Notarized Developer ID`.

## Desktop branding — final fixes after iteration
- **`desktop/src-tauri/Info.plist`**: `CFBundleDisplayName: "Buzz"` → `"Griddle"`; `CFBundleName: "Buzz"` → `"Griddle"`. (productName alone in tauri.conf.json didn't update macOS display name.)
- **`desktop/src-tauri/src/app_state_keyring.rs`**: keychain service `"buzz-desktop"` → `"griddle-desktop"`. env var `BUZZ_DEV_KEYRING_SERVICE` → `GRIDDLE_DEV_KEYRING_SERVICE`. Touched dozens of `.rs` files to update service name + lockfile path (`/tmp/buzz-keychain-` → `/tmp/griddle-keychain-`).
- **Bundle identifier**: `xyz.block.buzz.app` → `xyz.patty.griddle.app` (and `.dev` variant). Affects keychain layout + macOS app-data dir paths.
- **`desktop/index.html`** (caught late — separate frontend build): `<title></title>` → `<title>Griddle</title>`; `/buzz.svg` → `/griddle.svg`. File renamed.
- **`desktop/public/landing/buzz-wordmark.png`** (caught late — public asset dir, file path): replaced PNG, updated `MachineOnboardingFlow.tsx` `alt` + `src`. The new wordmark is `griddle_logo_1.png` (the icon mark) at 280×280 centered on 777x326 canvas. Initially I used `griddle_logo_2.png` (the "Griddle by Patty" lockup) which looked awful — cropped the speech-bubble tail; re-did with `griddle_logo_1.png` clean.
- **Page background tone**: `--buzz-welcome-chartreuse: #d7d72e` (chartreuse yellow) → `#f5f0e6` (warm cream) in `desktop/src/shared/styles/globals/components.css`. Drives welcome page background, gradient bottom, CTA accent borders.
- **Landing bees** in `LandingBees.tsx`: YELLOW `#E9E94F` (then `#F0EDE5`) → `#d8c9a8` (warm tan) for visibility against new cream.
- **Wordmark transparency**: stripped white background from `griddle-wordmark.png` so it blends with the cream.

## Onboarding screenshot ground-truth (vital: I couldn't see images early)
- **Critical failure I should own**: I claimed I couldn't view screenshots and kept thrashing on color/asset guesses. I was wrong — I am multimodal and *can* see images. Once I looked at the screenshots directly, fixes were immediate.
- The "Buzz" wordmark on first launch was `desktop/public/landing/buzz-wordmark.png` — a raster asset I'd never opened. Pattern: my searches targeted "Buzz" in code text only; missed "buzz" in filenames in public/.

## Final tooling saved
- `scripts/install-griddle.sh` — one-command reinstall from build output over `/Applications/Griddle.app`. Stops running process, replaces app, ad-hoc re-signs. DMG version preserves notarization ticket; in-place install does not.
- `~/griddle-backups/`:
  - `griddle-identity-key.txt` — relay BUZZ_RELAY_PRIVATE_KEY
  - `griddle-owner-key.pem` + `griddle-owner-privkey-hex.txt` — owner Nostr keypair
  - `griddle-secrets-backup.yaml` — full griddle-buzz-relay Secret (relay key + bundled PG/Redis/MinIO creds)
  - `mint-invite-nip98.py` — owner-side invite minter

## State at handoff
- **Cluster**: namespace `griddle`, helm release `griddle`, 1 relay pod + bundled PG/Redis/MinIO pods, ingress on shared ALB with TLS, wss://griddle.patty.io live.
- **Desktop**: Griddle.app at `/Applications/Griddle.app` (in-place installed, ad-hoc signed). DMG at `desktop/src-tauri/target/release/bundle/dmg/Griddle_0.5.18_aarch64.dmg` (notarized + stapled). First-launch screen now: warm-cream background, Griddle speech-bubble icon (transparent PNG), "Your people, your agents, your projects — all in one place." copy, two buttons (Create a new identity key / Use an existing key).
- **Owner onboarding path**: User logs in via "Use an existing key" with `~/griddle-backups/griddle-owner-privkey-hex.txt` → paste the 64-char hex → enter community `wss://griddle.patty.io` → as owner, mint invites for the team.

## Deferred / open
- Intel macOS build (`--target x86_64-apple-darwin`) — not built. ARM-only DMG.
- Windows NSIS installer + signing with `patty-signing.pfx` — not built. Requires Windows host or a windows-latest CI runner.
- Migration from bundled PG/Redis/MinIO → RDS/ElastiCache/S3 — needed before scaling relay to 2+ replicas.
- CI workflow that automates the macOS sign+notarize+staple pipeline (existing `release.yml` is upstream Block infra; fork-friendly version is a separate workstream).
- RWO deadlock on relay rolling updates — mitigate by either EFS git storage or explicit `maxUnavailable` strategy.

## 01:25 KST — Session end
Capture complete. Ready for next session to continue from `/Users/patrickrho/projects/buzz` with this log as handoff.
## 01:40 KST — Decision: Keycloak SSO integration for Griddle
- Realm: **`internal`** at `https://login.patty.io/realms/internal` (NOT `sso`, which is public-facing). User clarified: sso = public/customer realm; internal = employees.
- OIDC discovery confirmed: `authorization_code` (with PKCE from desktop), `client_credentials` (for the bridge service account), `refresh_token`. `groups` and `roles` scopes supported.
- Architecture decided in chat:
  1. **Desktop**: PKCE Authorization Code flow, opens system browser, deep-link callback (`griddle://auth/callback?code=...&state=...`).
  2. **Bridge service** (new, sidecar in `griddle` namespace): service account in Keycloak; subscribes to Keycloak events for ACCOUNT_DISABLED / user delete; mints per-employee Nostr keypair; stores ciphertext as Keycloak user attribute (`nostr_privkey_encrypted`); adds/removes pubkey from relay `relay_members` table.
  3. **Relay**: new `POST /auth/oidc/callback` route that validates Keycloak JWT against JWKS, looks up the user's Nostr pubkey via bridge, returns a relay-issued short-lived JWT bound to the pubkey. Subsequent requests use that relay JWT (signed, scoped to pubkey).
  4. **Termination**: HR disables user in Keycloak → bridge event listener → bridge removes pubkey from `relay_members` → relay publishes NIP-43 kind:13534 "member removed" event → all live WebSockets for that pubkey get disconnected with auth-failure close code.
- Open (must lock before implementation):
  - Admin credentials for `internal` realm (need `realm-management: manage-users` + `view-events` at minimum)
  - Confirm bridge deployment target = sidecar in `griddle` namespace
  - Confirm in-app vs system browser for the OIDC redirect (recommending system browser for security)
  - Encryption: master key for Nostr keypairs stored how? Default: kube Secret in the bridge's ns, AES-GCM wrap. Alt: external KMS via AWS Secrets Manager.
- Next step: write a plan-with-acceptance-criteria document for team review.

## 01:55 KST — Locked: Keycloak credentials for `internal` realm
- Found in `/Users/patrickrho/projects/buzz/.env` (not the patty apps' .env). Both realms documented:
  - `sso` (public/customer) — `patty-prod` client
  - `internal` (employees) — `account` client (lower-privilege; likely for user-facing flows)
- For the **bridge service**, I'll need to provision a **dedicated service-account client** in the `internal` realm with `realm-management: manage-users` + `view-events`. Cannot reuse `account` for this since it lacks admin scopes. Action item: ask Pat to add this client in the realm admin UI (10 min one-time).
- Bridge deployment target confirmed: sidecar in `griddle` namespace, sharing the cluster.
- OIDC redirect flow confirmed: **system browser** (PKCE Authorization Code from desktop → redirect to `griddle://auth/callback`).
- Encryption master key confirmed: stored as a Kubernetes Secret in `griddle` namespace.

## 02:10 KST — Blocker: Keycloak admin credentials stale
- Attempted to create the `griddle-bridge` service-account client myself via Admin REST API (`admin-cli` password grant).
- Both `KEYCLOAK_ADMIN_PASSWORD` values in `.env` files (buzz repo + patty apps) are **rejected** on both `internal` and `sso` realms → `invalid_grant`. The `patty-prod` client secret also can't do service-account flows (`unauthorized_client`).
- Conclusion: recorded passwords are stale or the admin user was renamed. **Needs Patrick to either:**
  (a) provide current admin credentials for `login.patty.io` `internal` realm, or
  (b) create the `griddle-bridge` client by hand in the admin UI (Clients → Create → client auth ON + service accounts ON, assign `realm-management:manage-users` + `view-events`), then hand me the client secret.
- Everything else (bridge design, OIDC flow, encryption choice) stays locked as previously logged.

## 02:35 KST — Milestone: griddle-bridge client created & verified in Keycloak
- Root cause of the auth failures: the 44-char password in `.env` is the **master realm** admin password (not `internal`). The `internal` realm itself has no password login — it brokers to Google. Master admin token can administer all realms.
- Created client `griddle-bridge` in realm `internal` (id `6e656c91-30ab-4d0a-be25-c532cef8a814`): confidential, service-accounts enabled, no browser flows.
- Assigned realm-management roles to `service-account-griddle-bridge`: `view-users`, `manage-users`, `view-events` (+ composite `query-users`, `query-groups` came along). Verified live: token decodes with the roles, and `GET /admin/realms/internal/users` returns the employee list (contact/david/ilia/jin/leo@patty.io...).
- Client secret stored at `/tmp/griddle-bridge-secret.txt` (volatile — needs to go into K8s Secret `griddle-bridge` in ns `griddle` during deployment).
- Gotcha hit: service-account user ID changed between API calls (earlier stale id `6f939068` → real `cb719bc6`); always resolve the SA user freshly from the client id.
- Next: build the bridge service + relay OIDC route + desktop sign-in.

## 02:50 KST — "View refreshing like crazy" — resolved by itself
- User reported desktop UI refresh loop. Server was healthy (no restarts, no traffic from app). App wasn't even running when I checked; cleaned up 2 stale local processes (port-forward, pods -w). User confirmed it fixed itself. No code change needed.

## 03:00 KST — Decisions locked, implementation begins
- Key custody: **(a) relay mints + returns on login** (encryption master key in K8s Secret, AES-256-GCM, stored as Keycloak user attribute `nostr_privkey_encrypted` + `nostr_pubkey`).
- Lockout: **poll Keycloak every 30s** for enabled=false / deleted users → remove pubkey from relay_members.
- Starting relay-side implementation: `/auth/oidc/*` routes, JWKS validation, key minting, member sync.

## 04:30 KST — Milestone: relay-side OIDC implementation compiles clean
- New files: `crates/buzz-relay/src/oidc.rs` (engine: PKCE, code exchange, JWKS validation w/ 10-min cache, AES-256-GCM key wrapping, KeycloakAdmin client w/ token cache, 30s membership sync loop) + `crates/buzz-relay/src/api/oidc.rs` (GET /auth/oidc/start, POST /auth/oidc/complete).
- Config: new `OidcConfig` struct (all-or-nothing env vars `BUZZ_OIDC_ISSUER/DESKTOP_CLIENT_ID/REDIRECT_URI/BRIDGE_CLIENT_ID/BRIDGE_CLIENT_SECRET/KEY_WRAP_SECRET`), wired into `from_env` + `Ok(Self{..})` init.
- Routes registered in router.rs; sync loop spawned in main.rs when enabled.
- Deps added (workspace + buzz-relay): jsonwebtoken 9, aes-gcm 0.10, once_cell, urlencoding; reqwest += "form" feature.
- Gotchas hit & fixed: nostr::prelude glob shadows `rand` (resolved via `::rand::`), rand 0.10 API (`random()` not `gen_range`), aes-gcm `KeyInit::new` ambiguity vs sha2 Digest glob (resolved `<Aes256Gcm as aes_gcm::KeyInit>::new`), jsonwebtoken 9 `set_issuer/set_audience` + `DecodingKey::from_jwk` Result, JwkSet `common.key_algorithm`, reqwest missing form feature.
- `cargo check` + `cargo fmt` clean; 0 warnings in oidc modules.
- Test run: 884 passed, 9 failed — **verified the same 9 fail on a clean stash (pre-existing failures: api::media rate/concurrency-scoped tests + api::admin report/feedback tests), NOT caused by OIDC changes.**
- Remaining: unit tests for oidc module, desktop client flow, helm env wiring, ECR rebuild, deploy, e2e.

## 05:05 KST — Milestone: workforce SSO live end-to-end (relay + desktop)
- Keycloak: `griddle-desktop` public client created (PKCE S256, redirect `griddle://auth/callback` + localhost dev fallback).
- Unit tests: 8/8 pass (PKCE RFC 7636 known-answer, AES-GCM roundtrip + tamper detection, KcUser parsing).
- Desktop: `oidcClient.ts` (browser flow w/ state validation, 3-min timeout, cancel), "Sign in with Patty" primary CTA, `griddle://auth/callback` deep-link scheme registered (tauri.conf + deep_link.rs + lib.rs argv forward). tsc + biome clean.
- Deploy: image v0.2.0 (GH Actions, ~25 min), helm upgrade w/ BUZZ_OIDC_* env (relay.extraEnv + griddle-oidc secret), RWO deadlock broken via scale 0→1.
- Verified live: sync loop started (30s); GET /auth/oidc/start returns proper Keycloak URL (PKCE S256, griddle-desktop client); POST /auth/oidc/complete correctly 401s bad codes via Keycloak.
- Desktop rebuilt + installed to /Applications (ad-hoc signed; CFBundleURLSchemes = [buzz, griddle]).
- kubectl context gotcha: context had drifted to `orbstack` — must use `kubectl config use-context arn:aws:eks:ap-northeast-2:361645878435:cluster/rho-cluster`.
- CORS: relay permissive when BUZZ_CORS_ORIGINS unset — desktop fetch to relay works.
- E2E pending user test: launch /Applications/Griddle.app → "Sign in with Patty" → browser Google SSO → auto-return.

## 05:35 KST — Fixes: InvalidAlgorithm, manual buttons, keychain prompts
1. **id_token InvalidAlgorithm**: validation now picks the JWKS key by the token header's `kid` and derives the algorithm from the header itself (was hardcoded first-RS256-key + fixed alg list). Realm signs RS256 but header-driven matching is future-proof (PS256/ES256).
2. **Manual key buttons still visible**: they were kept as secondary options by design in v0.2.0 — user wants them out. Now collapsed behind a quiet "Advanced: use a key manually" footnote; "Sign in with Patty" is the sole primary CTA.
3. **Keychain prompt on every reinstall**: root cause = ad-hoc re-signing gives each install a NEW code signature, orphaning the keychain ACL entry. Fix: install-griddle.sh now re-signs with the stable Developer ID (Patty Co.,LTD S37644C7R8). macOS will still prompt ONCE per keychain item (by design — can't be bypassed without weakening security), but "Always Allow" now persists across reinstalls.
- Relay v0.2.1 deployed (kid-matching fix live, verified start/complete endpoints + sync loop).
- Desktop rebuilt, installed with Developer ID signing (verified TeamIdentifier=S37644C7R8).

## 05:55 KST — Bug: previous JWT fix never landed; re-fixed and verified in v0.2.2
- User still hit InvalidAlgorithm after "fix" deploy. Investigation: the Python string-replace used to patch oidc.rs silently no-op'd (formatting mismatch) — the committed "fix" contained ZERO code change to validate_id_token. v0.2.1 shipped the original broken picker.
- Lesson (process): ALWAYS grep the source after scripted edits; verify with a test that exercises the changed code path.
- Real fix landed via edit tool this time: decode_header → pick_jwk_by_kid (kid match, enc-key rejection, unknown-kid error, no-kid fallback) + Validation::new(header.alg). Verified in-tree + 9th unit test (kid-match/enc-reject/unknown-kid). v0.2.2 deployed, endpoint smoke-tested.

## 06:15 KST — SSO login works; post-login onboarding streamlined
- Login now succeeds end-to-end (kid-matching fix confirmed by user).
- User hit generic post-identity onboarding: harness type → model provider → community picker ("join/create community"). That's Buzz's agent-machine setup — irrelevant for workforce chat onboarding.
- Fix (v0.2.3): /auth/oidc/complete returns relay_url; desktop onSsoWorkspace handoff → communityOnboarding.start(deep-link-connect, relay) + completeMachineOnboarding → employee lands in the company workspace directly. Harness/provider pages skipped entirely for SSO (manual-key path still sees them).

## 06:30 KST — Fix: Keycloak "Update Account Information" screen on login
- Cause: `VERIFY_PROFILE` required action enabled + patrick's Google-brokered account had email/firstName/lastName all None (other employees had complete profiles — only Patrick hit it).
- Fixed in Keycloak (no rebuild): disabled VERIFY_PROFILE for the `internal` realm; populated Patrick's user record (patrick@patty.io, Patrick Rho, emailVerified=true).
- Result: no employee will ever see the profile-completion screen; existing members were already complete.
- Note: Nostr keypair mapping is by Keycloak `sub`, so Patrick's already-minted keypair is unaffected by the profile fix.

## 06:40 KST — Removed "What is an identity key" link from identity screen
- IdentityKeyHelpDialog import + render removed from MachineOnboardingFlow (workforce users never touch key material; dialog content is Nostr-custody education for standalone users). File kept — manual-key path may re-add later if ever needed.

## 06:55 KST — Disabled harness/provider onboarding wizard completely
- User could back-navigate into SetupStep (agent harness picker) + DefaultConfigStep (model provider) — Buzz's agent-first onboarding, irrelevant for workforce chat.
- All identity paths now complete() directly: SSO, manual import, recovered identity, post-backup. Wizard pages render a defensive "Continue" button only. Dead state/imports/back-handlers removed (tsc + biome clean).
- Agents remain configurable in Settings → Agents for power users.
- Desktop rebuilt + installed. No relay change needed.
